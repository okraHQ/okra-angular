import { Angular8OkraDirective } from './angular8-okra.directive';

describe('Angular8OkraDirective', () => {
  it('should create an instance', () => {
    //const directive = new Angular8OkraDirective();
    //expect(directive).toBeTruthy();
  });
});
